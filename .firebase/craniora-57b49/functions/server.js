const { onRequest } = require('firebase-functions/v2/https');
  const server = import('firebase-frameworks');
  exports.ssrcraniora57b49 = onRequest({"region":"asia-southeast2"}, (req, res) => server.then(it => it.handle(req, res)));
  